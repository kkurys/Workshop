export class User {
    constructor(
        public id: string, 
        public firstName: string, 
        public lastName: number,
        public email: string,
        public userName: string) { }
}